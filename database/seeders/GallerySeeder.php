<?php

namespace Database\Seeders;

use Illuminate\Database\Seeder;

class GallerySeeder extends Seeder
{
    /**
     * Previously generated 10 fake albums (Faker lorem-ipsum titles/captions,
     * picsum.photos placeholder images) and truncated the tables on every
     * run — wiping real data. Real gallery content is seeded by
     * GalleryExternalSeeder instead. Left as a no-op rather than deleted so
     * DatabaseSeeder's call list doesn't break; do not re-add fake generation
     * here.
     */
    public function run(): void
    {
        //
    }
}
